import { TituloStyleDirective } from './titulo-style.directive';

describe('TituloStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new TituloStyleDirective();
    expect(directive).toBeTruthy();
  });
});
